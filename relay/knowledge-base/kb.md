# FIT College Knowledge Base

<!--
  Placeholder. Replaced by the Admin Console export.
  Loaded at startup and on POST /admin/reload-kb (server.js -> loadKnowledgeBase()).
  Injected into the system prompt via the {{KNOWLEDGE_BASE}} token.
-->

_Knowledge base not yet populated. Export the latest content from the Admin Console to replace this file, then call `POST /admin/reload-kb` to hot-reload without a restart._
